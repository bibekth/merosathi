<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/off_white"
    android:padding="24dp">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/body_change"
        android:contentDescription="Body Changes"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Track Body Changes"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/text_gray"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Understand your body changes week by week and get helpful tips for a healthy pregnancy."
        android:textSize="16sp"
        android:textColor="@color/text_gray"
        android:gravity="center"/>
</LinearLayout>
